# Agent Skill Vault

Use this repository as a multi-domain skill source and MCP server.

For project work, install pinned skills into the target project first, then follow the target project's `CLAUDE.md`, `.claude/skills`, and `.agents/skills.lock.json`. Save durable decisions to `.agent-plans/decisions/`, ADRs, or project docs when finishing meaningful work.
